<template>
  <div class="content">
    下面是 占位的文字 
<p>    are multiple modules with names that only differ in casing.
This can lead to unexpected behavior when compiling on a filesystem with other case-semantic.
Use equal casing. Compare these module identifiers:
* G:\wamp\微信开发\jdProduct\node_modules\._vue-style-loader@2.0.5@v</p>
<p>    are multiple modules with names that only differ in casing.
This can lead to unexpected behavior when compiling on a filesystem with other case-semantic.
Use equal casing. Compare these module identifiers:
* G:\wamp\微信开发\jdProduct\node_modules\._vue-style-loader@2.0.5@v</p>
<p>    are multiple modules with names that only differ in casing.
This can lead to unexpected behavior when compiling on a filesystem with other case-semantic.
Use equal casing. Compare these module identifiers:
* G:\wamp\微信开发\jdProduct\node_modules\._vue-style-loader@2.0.5@v</p>
<p>    are multiple modules with names that only differ in casing.
This can lead to unexpected behavior when compiling on a filesystem with other case-semantic.
Use equal casing. Compare these module identifiers:
* G:\wamp\微信开发\jdProduct\node_modules\._vue-style-loader@2.0.5@v</p>
<p>    are multiple modules with names that only differ in casing.
This can lead to unexpected behavior when compiling on a filesystem with other case-semantic.
Use equal casing. Compare these module identifiers:
* G:\wamp\微信开发\jdProduct\node_modules\._vue-style-loader@2.0.5@v</p>
<p>    are multiple modules with names that only differ in casing.
This can lead to unexpected behavior when compiling on a filesystem with other case-semantic.
Use equal casing. Compare these module identifiers:
* G:\wamp\微信开发\jdProduct\node_modules\._vue-style-loader@2.0.5@v</p>
  </div>
</template>
<style scoped>
  .content {
    font-size:12pt;
  }
</style>
<script>
  export default {
  }

</script>
